ДЗ 19.02.2026

Stack<string> action = new Stack<string>();
action.Push("Лечь");
action.Push("Сесть");
action.Push("Пойти");
Console.WriteLine($"первое действие {action.Peek()}");
while (action.Count > 0)
{
    Console.WriteLine($"Отменено: {action.Pop()}");
    if (action.Count > 0);       
}
